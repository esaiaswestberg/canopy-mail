session:
  name: canopy-mail
  windows:
    - name: development
      panes:
        - name: claude
          commands:
            - export _ZO_DOCTOR="0"
            - unalias cd
            - clear && claude
        - name: runner
          command: clear && air
        - name: project
          command: clear && echo Welcome to Canopy Mail!
      layout:
        columns:
          - claude
          - rows:
            - runner
            - project